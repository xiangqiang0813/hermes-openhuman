# 我合并了 OpenClaw 的人格、Hermes 的技能、JiuwenSwarm 的头脑——这是我的 AI Agent 框架 OpenHuman

> 一个框架的人格，另一个框架的技能，第三个框架的团队协作。我把它们拼在了一起。

---

## 一、为什么我不满足于任何一个框架

### 我试过 OpenClaw

25 万 GitHub 星标的人格系统确实好。SOUL.md 定义了 AI 的"灵魂"，USER.md 记录了用户画像。**但它有两件事做不好：**

- 没有多平台网关。我只能开着终端跟它聊，没法接飞书、企微、Telegram
- 技能系统不够强。虽然有 Skill 概念，但缺乏自学机制和自动匹配

### 我试过 Hermes Agent

Nous Research 做的，**技能自学和跨平台网关是目前所有框架里最强的**。22 个消息平台，自动从任务中萃取技能，跨会话记忆。

**但它缺什么？**

- 人格定义非常弱。你说"你是一个 helpful assistant"，就没了。没有 SOUL.md 那种深层的人格设计
- 没有开箱即用的人格模板库

### 我看了 JiuwenSwarm

华为支持的 openJiuwen 社区刚发布的，提出了 Coordination Engineering 的范式，多 Agent 团队协作确实漂亮。**但它刚发布，而且绑定华为云。**

### 我看了 Karpathy 的 LLM Wiki

结构化的 Markdown 记忆文件设计理念很好——AI 可读可编辑。但它的记忆需要**手动维护**，没有自动化。

### 所以我自己做了一个

不是重新发明轮子，是把这些框架最好的部分**组合到一起**，再解决它们各自没解决的问题。

这就是 **OpenHuman**。

---

## 二、OpenHuman 的架构设计

### 整体架构（我画了张图）

```
人格层
────────────────────────────────────────────
SOUL.md  务实工程师人格 · 14种人格切换
                     │
记忆层
────────────────────────────────────────────
记忆树分块  ≤3000 token · SHA256指纹去重
4000 token 硬预算 · 自动轮询同步 · TokenJuice压缩
                     │
技能层
────────────────────────────────────────────
YAML frontmatter + markdown · 自动场景匹配
自学机制  动态新增技能
                     │
数据通道
────────────────────────────────────────────
飞书 CLI（工作）   │   Obsidian（知识）
文档│日历│任务│通讯     笔记│知识库│写作
                     │
调度层
────────────────────────────────────────────
cronjob 调度 · Gateway 网关 · TokenJuice 智能压缩
```

四层架构，每层解决一个特定问题。

---

## 三、关键技术决策

### 记忆系统：从 Karpathy 的 LLM Wiki 灵感出发

Karpathy 提出用 Markdown 文件做 AI 的记忆存储——结构清晰、AI 可读可编辑。但问题是：**手动维护不可持续。**

我的做法：

```python
# 记忆树分块
每条长对话 → 拆成 ≤3000 token 的分块
每个分块 → 独立 Markdown 文件，自动命名归档
自动轮询，有新内容就追加

# SHA256 指纹去重
content_hash = hashlib.sha256(content.encode()).hexdigest()
# 写入前检查指纹是否存在 → 存在则跳过
# 从 SQLite 改为纯指纹比较，更轻量

# 4000 token 硬预算
当总记忆 > 4000 token → 按重要性评分裁剪
保持响应速度，不会爆上下文
```

### 数据通道：飞书 CLI + Obsidian，覆盖工作和个人

OpenHuman 最初想接 118 个第三方 API——各走各的认证、各用各的 SDK。**维护成本爆炸。**

后来做了关键决策：**只接两个数据底座。**

```yaml
# 工作数据 → 飞书 CLI
lark-cli docs       → 文档管理
lark-cli calendar   → 日历事件
lark-cli task       → 任务系统
lark-cli im         → 即时通讯
lark-cli base       → 多维表格
lark-cli minutes    → 会议纪要

# 个人知识 → Obsidian
Obsidian Git 自动同步 → 知识库实时更新
60s 间隔 auto commit+push → PC+Android 双向同步
Markdown 格式 → AI 直接可读可写
```

飞书管工作，Obsidian 管知识。**两个数据源覆盖了 90% 的信息场景。** 从 118 个集成降到 2 个，复杂度从蜘蛛网变成了两条管道。

### 人格系统：从 AI-Agents-ZH 社区借鉴

SOUL.md 定义了 AI 的"灵魂"——不只是"你是一个助手"，而是：

- **人格边界**：什么该做什么不该做
- **行为守则**：遇到问题时怎么反应
- **沟通风格**：用数据说话、不装懂、直接给结论

我的改动是：
- 把通用人格模板改成了**务实工程师人格**（数据驱动、成本意识、工程化思维）
- 14 种人格随时切换（kawaii、noir、pirate、shakespeare…）
- 根据关键词自动匹配对应人格和技能

### 技能系统：直接复用 Hermes 的 Skill 机制

Hermes 的 Skill 格式（YAML frontmatter + markdown body）和自学机制直接拿来用，不改它的结构性设计。改动只有：

- 格式适配：社区标准 → Hermes SKILL.md
- 匹配逻辑：手动触发 → 自动关键词路由

---

## 四、和各个框架的对比

| 能力 | OpenClaw | Hermes | JiuwenSwarm | **OpenHuman** |
|------|----------|--------|-------------|---------------|
| 人格深度 | ✅ SOUL.md | ❌ 弱 | ❌ 无 | ✅ 继承+优化 |
| 多平台网关 | ❌ | ✅ 22个 | ❌ 飞书仅 | ✅ |
| 技能自演进 | ❌ | ✅ | ✅ | ✅ |
| 记忆持久化 | ✅ | ✅ | ❌ | ✅ 树分块+指纹 |
| Token 控制 | ❌ | ❌ | ❌ | ✅ 4000硬预算+压缩 |
| 多 Agent | ❌ | ❌ 委托 | ✅ Swarm | 🚧 规划中 |
| 数据集成 | 零散 | 零散 | 飞书 | ✅ 统一飞书 CLI |
| 部署 | 本地 | 本地/云 | 华为云 | 本地/云 |

---

## 五、一些踩过的坑

### 1. 不要重写，要复用

最开始我想用 Rust 重写整个系统——快，安全，优雅。写了两个月放弃了。**你一个人不可能重写三个框架的东西。**

最终方案：Hermes cronjob + Python 脚本，重写量降为 0。

### 2. 不要自己做集成，找统一数据底座

118 个集成看似什么都能接，实际是什么都接不稳。**一个飞书 CLI 把复杂度从 O(n) 降到了 O(1)。**

### 3. 记忆要设硬上限

"无上限"听起来好，实际是上下文爆炸、响应变慢、成本失控。**4000 token 硬预算 + 按重要性裁剪**反而让体验更稳定。

---

## 六、下一步

- 把开源仓库整理好，放 GitHub
- 接入 JiuwenSwarm 的 Swarm Skill 规范，让 Agent 能团队协作
- 写更多开箱即用的人格模板

如果你也在做类似的 Agent 框架，欢迎来交流。我的联系方式在 GitHub。

---

*OpenHuman — 一个有人格、会学习、能跨平台的 AI Agent 框架。*

*GitHub: https://github.com/xiangqiang0813/OpenHuman*
